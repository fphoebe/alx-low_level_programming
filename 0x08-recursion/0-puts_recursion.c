 #include "main.h"
   /**
     *main - puts recursion
     *
     *Return: always 0
     */
char _puts_recursion factorial(char *s)
{
	if (*s == 0)
	return 0;
}
if (*s == 1)
{ return 1
}
return *s * factorial(n-1);
}
int main()
{
	putchar("%c\n", factorial ());
	return (0);
}
