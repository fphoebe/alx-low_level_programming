#include <stdio.h>
/**
 * main - print alphabets in lowercase in reverse
 *
 * Return: always 0
 */
int main(void)
{
	char lowercase;

	for (lowercase = 'z'; lowercase >= 'a'; lowercase--)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
