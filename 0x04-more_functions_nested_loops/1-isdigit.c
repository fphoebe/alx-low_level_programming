/**
 * print - _isdigit - A program that prints the last digit
 * of a number.
 *
 * @n: input number as an integer.
 *
 * Return:is digit.
 */
int _isdigit(int i)
{
	int i;

	if (i >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
