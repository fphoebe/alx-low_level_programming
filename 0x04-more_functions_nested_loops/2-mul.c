#include "main.h"
/**
  *main -entry point 
  *int-mul - prints two integers multipying
  *Return -always 0
  */
int mul(int a, int b)
{
	int a;
	int b;
	int mul;

	for (mul = a*b)
	{
		_putchar(mul);
	
	}
	return (0)
}
